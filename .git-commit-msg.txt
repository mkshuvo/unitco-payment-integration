feat(integration,api-keys,web,infra): Unit SDK status check, API key mgmt, /status page; compose uses .env

- Integration (backend)
  - Add Unit status endpoint `GET /integration/unit/status` via `IntegrationController`
  - Implement `UnitService.checkStatus()` using `@unit-finance/unit-node-sdk` and `customers.list({ limit: 1 })`
  - 5s timeout, detailed UP/DOWN view incl. target and reason
- API Keys (backend)
  - Add encrypted API key management: entity, service, controller (CRUD + activate)
  - AES-GCM encrypt/decrypt with masked display and SHA-256 fingerprint
  - `getActivePlaintext()` supplies runtime key to Unit SDK (no logging of secrets)
- Web (frontend)
  - Add `/status` page that calls `GET /integration/unit/status` via Next rewrite
  - `web/next.config.mjs` rewrites `/integration/*` to API (api:3000 in Docker)
- Infra
  - `docker-compose.yml`: use `env_file: ./.env`; remove hardcoded UNIT_API_KEY from compose
  - Keep `UNIT_BASE_URL=https://api.s.unit.sh`, `UNIT_STATUS_CHECK_PATH=/identity`
- Config/Deps/Docs
  - `src/config/env.ts` updated to validate optional Unit envs
  - Ensure `@unit-finance/unit-node-sdk` present in root `package.json`
  - Update `docs/progress.md` with instructions and current status

Current behavior
- Status page renders but shows DOWN when no active API key is present in DB.
- Cause: `UnitService.checkStatus()` could not load active API key (by design it reads from DB, not env).

How to enable UP
1) Create an API key: POST /api/keys { name, secret: <YOUR_UNIT_API_KEY> }
2) Activate it: POST /api/keys/:id/activate
3) Refresh http://localhost:56483/status

Security
- `.env` is gitignored; compose reads it via `env_file`. No secrets committed.

Notes
- Honors preference: SDK-based status check with 5s timeout; no raw HTTP.
